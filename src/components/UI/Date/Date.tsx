import { FC } from 'react';

import classes from './Date.module.scss';

type Props = {};

// todo сделать каждой иконке свой компонент

const Date: FC<Props> = ({}) => {
	return <span></span>;
};
export default Date;
